version: "3.5"
services:
  haproxy:
    container_name: ctf-haproxy
    image: ctf-haproxy:2.3
    build:
      context: ./
    environment:
      STATS_PANEL_USERNAME: ctf #$STATS_PANEL_USERNAME
      STATS_PANEL_PASSWORD: ctf #$STATS_PANEL_PASSWORD
    network_mode: host


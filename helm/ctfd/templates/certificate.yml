apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: ctfd-certificate
  namespace: istio-ingress
spec:
  secretName: ctfd-tls
  dnsNames:
  -  ctfd.loopset.net
  issuerRef:
    name: letsencrypt
    kind: ClusterIssuer
